<?xml version="1.0" encoding="UTF-8"?>
<api context="/centro-custo" name="HealthCheckAPI" xmlns="http://ws.apache.org/ns/synapse">
    <resource methods="GET"    uri-template="/v1/centro-custo">
    <inSequence>
        <log level="custom">
            <property name="Requisição recebido" value="/v1/centro-custo"/>
        </log>
        <property name="HTTP_SC" scope="axis2" type="STRING" value="200"/>
        <respond/>
    </inSequence>
    <outSequence/>
    <faultSequence/>
</resource>           
<resource methods="GET"    uri-template="/v1/centro-custo/{estabelecimento}/{codigo}">
    <inSequence>
        <log level="custom">
            <property name="Requisição recebido" value="/v1/centro-custo/{estabelecimento}/{codigo}"/>
        </log>
        <property name="HTTP_SC" scope="axis2" type="STRING" value="200"/>
        <respond/>
    </inSequence>
    <outSequence/>
    <faultSequence/>
</resource>           
<resource methods="GET"    uri-template="/v1/centro-custo/{descricao}">
    <inSequence>
        <log level="custom">
            <property name="Requisição recebido" value="/v1/centro-custo/{descricao}"/>
        </log>
        <property name="HTTP_SC" scope="axis2" type="STRING" value="200"/>
        <respond/>
    </inSequence>
    <outSequence/>
    <faultSequence/>
</resource>           

</api>
